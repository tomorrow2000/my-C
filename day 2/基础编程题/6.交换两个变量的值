#include <stdio.h>

void swap(int *a,int *b)
{
    int c;
    c=*a;
    *a=*b;
    *b=c;
}

int main(int argc,char argv[])
{
    int a,b;
    printf("请输入两个整数(例如1 2):");
    scanf("%d %d",&a,&b);
    swap(&a,&b);
    printf("%d %d\n",a,b);
    return 0;
}
