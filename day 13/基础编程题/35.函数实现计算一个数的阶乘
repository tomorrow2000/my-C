#include <stdio.h>

long long factorial(int n)
{
    if(n>0)
    {
        return n*factorial(n-1);
    }else
    {
        return 1;
    }
}

int main(int argc,char argv[])
{
    int n;
    scanf("%d",&n);
    long long ret=factorial(n);
    printf("%d",ret);

    return 0;
}
