#include <stdio.h>

int main(int argc,char argv[])
{
    int n=0;
    scanf("%d",&n);
    for(int i=1;i<n+1;i++)
    {
        for(int j=1;j<=i;j++)
        {
            printf("%d*%d=%-2d ",j,i,i*j);
        }
        printf("\n");
    }
    return 0;
}
