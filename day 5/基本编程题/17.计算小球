#include <stdio.h>

int main(int argc,char argv[])
{
    float h=0,n=0,sum=0;
    scanf("%f %f",&h,&n);

    for(int i=0;i<n;i++)
    {
        sum+=h;
        h/=2;
        if(i<n-1)
        sum+=h;
    }
    printf("%.1f %.1f",sum,h);


    return 0;
}
