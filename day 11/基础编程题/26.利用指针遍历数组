#include <stdio.h>

int main(int argc,char argv[])
{
    int a[6]={0};
    int *p=a;
    for(int i=0;i<6;i++)
    {
        scanf("%d",p+i);
    }
    for(int i=0;i<6;i++)
    {
        printf("%d ",*(p+i));
    }

    return 0;
}
