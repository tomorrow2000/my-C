#include <stdio.h>

int main(int argc,char argv[])
{
    int a[6]={0},min,max;
    for(int i=0;i<6;i++)
    scanf("%d",&a[i]);
    min=a[0];max=a[0];
    for(int i=0;i<6;i++)
    {
        if(min>a[i])
        {
            min=a[i];
        }
        if(max<a[i])
        {
            max=a[i];
        }
    }
    printf("%d %d",min,max);

    return 0;
}
