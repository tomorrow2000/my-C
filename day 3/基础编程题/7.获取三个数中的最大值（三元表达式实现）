#include <stdio.h>

int main(int argc,char argv[])
{
    int a,b,c,max;
    printf("please input three num:");
    scanf("%d %d %d",&a,&b,&c);
    max = a>b? a:b;
    max = max>c? max:c;
    printf("%d",max);

    return 0;
}
