#include <stdio.h>

int main(int argc,char argv[])
{
    int a[4][3]={0},sum=0;
    for(int i=0;i<4;i++)
    scanf("%d,%d,%d",&a[i][0],&a[i][1],&a[i][2]);
    for(int i=0;i<4;i++)
    sum+=a[i][0]+a[i][1]+a[i][2];
    printf("%d\n",sum);
    return 0;
}
